services:
  jenkins-blueocean:
    image: myjenkins-blueocean
    container_name: jenkins-blueocean
    restart: on-failure
    networks:
      - jenkins
    environment:
      - DOCKER_HOST=tcp://docker:2376
      - DOCKER_CERT_PATH=/certs/client
      - DOCKER_TLS_VERIFY=1
      - TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock # For Testcontainers to use the host's Docker socket
      - DOCKER_TLS_VERIFY=0 # Disable TLS verification for Jenkins to communicate with the Docker daemonS
    ports:
      - "8081:8080"
      - "50000:50000"
    volumes:
      - jenkins-data:/var/jenkins_home
      - jenkins-docker-certs:/certs/client:ro
      - /var/run/docker.sock:/var/run/docker.sock

networks:
  jenkins:
    external: true

volumes:
  jenkins-data:
  jenkins-docker-certs:
